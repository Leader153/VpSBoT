# 📂 ПАСПОРТ ПРОЕКТА: Gemini Voice Bot (Leader)

## 🎯 Описание проекта
Голосовой AI-бот для компании "Leader" (Израиль).
**Основные направления:**
1.  Аренда яхт (Герцлия, Хайфа).
2.  Продажа кредитных терминалов (Nova).

**Язык:** Иврит (основной), Русский (по запросу).
**Год работы:** Строго 2026 (все даты бронирования только на 2026).

---

## 🛠️ Технический Стек
*   **Ядро:** Node.js + Express.
*   **Телефония:** Twilio (Voice & WhatsApp & SMS).
*   **AI:** Google Gemini 2.0 Flash (генерация ответов).
*   **Embeddings:** `text-embedding-004` (или `gemini-embedding-001` в зависимости от доступности).
*   **База знаний (RAG):** ChromaDB (запущена в Docker на порту 8000).
*   **Календарь:** Google Calendar API (Service Account).
*   **Запуск:** PM2 (`gemini-bot` + `leader-tunnel` Cloudflare).

---

## ⚙️ Ключевая Логика (Business Logic)

### 1. Процесс Бронирования Яхты (Booking Flow)
**Строгий порядок действий:**
1.  **Консультация:** Бот отвечает на вопросы, ищет в базе (RAG).
2.  **Медиа:** Если клиент просит фото, бот вызывает `send_whatsapp_message` и вставляет ссылку на фото в текст. Код перехватывает ссылку и шлет картинку.
3.  **Квалификация (Pre-booking):** Если клиент хочет заказать, бот **СНАЧАЛА** отправляет инструкцию по оплате (`send_closing_process_info`), чтобы клиент подтвердил условия.
4.  **Оформление:** Бот спрашивает Имя, Дату, Время.
5.  **Финал:** Бот вызывает `send_booking_confirmation`.
    *   Создает событие в Google Calendar.
    *   Отправляет клиенту в WhatsApp "лесенку" сообщений: Детали -> Ссылка на оплату -> Локация (Waze) -> Условия (разбиты на части).
    *   Отправляет владельцу Email с деталями.

### 2. Логика Календаря (Yacht Aliases)
Бот умеет проверять занятость, даже если в календаре написано не точное название.
Используется словарь синонимов (`YACHT_ALIASES`):
*   `Joy-BE` = "JOY", "JOYB", "ג'וי".
*   `Dolfin` = "Dolphin", "עסוק/ה".

### 3. WhatsApp и SMS
*   **Проблема 24 часов:** Если WhatsApp не уходит (клиент не писал первым), система автоматически шлет **SMS** со ссылкой на чат.
*   **Прием файлов:** Если клиент присылает фото чека (квитанцию), бот пересылает это фото владельцу в WhatsApp.

---

## 📂 Структура Файлов (Где что лежит)

*   `index.js` — Точка входа, загрузка .env.
*   **`handlers/answer_phone.js`** — Обработка голосового звонка. Логика "слушай -> думай -> говори". Управление музыкой ожидания.
*   **`handlers/messaging_handler.js`** — Прием входящих WhatsApp/SMS (и чеков).
*   **`calendar/calendarTools.js`** — **ГЛАВНЫЙ ФАЙЛ ИНСТРУМЕНТОВ**. Все функции для Gemini (проверка, бронь, отправка сообщений).
*   **`calendar/calendarService.js`** — Низкоуровневая работа с Google API (проверка синонимов яхт).
*   **`data/botBehavior.js`** — **МОЗГИ (Промпт)**. Системная инструкция, личность бота, правила поведения.
*   **`data/products_knowledge_base.csv`** — База данных товаров (Яхты, Цены, Ссылки, Локации).
*   **`scripts/loadDocuments.js`** — Скрипт очистки и загрузки базы в ChromaDB.
*   **`utils/whatsappService.js`** — Отправка WhatsApp (с поддержкой Медиа).
*   **`utils/smsService.js`** — Отправка SMS.

---

## 🚑 Инструкции по устранению проблем

### 1. Дубликаты в поиске / База "глючит"
Решение: Полная пересборка контейнера.
```powershell
docker stop chromadb
docker rm chromadb
docker run -d --name chromadb -p 8000:8000 chromadb/chroma
node scripts/loadDocuments.js
pm2 restart all

# 📂 ПАСПОРТ ПРОЕКТА: Gemini Voice Bot (Leader)

## 🎯 Описание проекта
Голосовой AI-бот для компании "Leader" (Израиль).
**Основные направления:**
1.  Аренда яхт (Герцлия, Хайфа).
2.  Продажа кредитных терминалов (Nova).

**Язык:** Иврит (основной), Русский (по запросу).
**Год работы:** Строго 2026 (все даты бронирования только на 2026).

---

## 🛠️ Технический Стек
*   **Ядро:** Node.js + Express.
*   **Телефония:** Twilio (Voice & WhatsApp & SMS).
*   **AI:** Google Gemini 2.0 Flash (генерация ответов).
*   **Embeddings:** `text-embedding-004` (или `gemini-embedding-001` в зависимости от доступности).
*   **База знаний (RAG):** ChromaDB (запущена в Docker на порту 8000).
*   **Календарь:** Google Calendar API (Service Account).
*   **Запуск:** PM2 (`gemini-bot` + `leader-tunnel` Cloudflare).

---

## ⚙️ Ключевая Логика (Business Logic)

### 1. Процесс Бронирования Яхты (Booking Flow)
**Строгий порядок действий:**
1.  **Консультация:** Бот отвечает на вопросы, ищет в базе (RAG).
2.  **Медиа:** Если клиент просит фото, бот вызывает `send_whatsapp_message` и вставляет ссылку на фото в текст. Код перехватывает ссылку и шлет картинку.
3.  **Квалификация (Pre-booking):** Если клиент хочет заказать, бот **СНАЧАЛА** отправляет инструкцию по оплате (`send_closing_process_info`), чтобы клиент подтвердил условия.
4.  **Оформление:** Бот спрашивает Имя, Дату, Время.
5.  **Финал:** Бот вызывает `send_booking_confirmation`.
    *   Создает событие в Google Calendar.
    *   Отправляет клиенту в WhatsApp "лесенку" сообщений: Детали -> Ссылка на оплату -> Локация (Waze) -> Условия (разбиты на части).
    *   Отправляет владельцу Email с деталями.

### 2. Логика Календаря (Yacht Aliases)
Бот умеет проверять занятость, даже если в календаре написано не точное название.
Используется словарь синонимов (`YACHT_ALIASES`):
*   `Joy-BE` = "JOY", "JOYB", "ג'וי".
*   `Dolfin` = "Dolphin", "עסוק/ה".

### 3. WhatsApp и SMS
*   **Проблема 24 часов:** Если WhatsApp не уходит (клиент не писал первым), система автоматически шлет **SMS** со ссылкой на чат.
*   **Прием файлов:** Если клиент присылает фото чека (квитанцию), бот пересылает это фото владельцу в WhatsApp.

---

## 📂 Структура Файлов (Где что лежит)

*   `index.js` — Точка входа, загрузка .env.
*   **`handlers/answer_phone.js`** — Обработка голосового звонка. Логика "слушай -> думай -> говори". Управление музыкой ожидания.
*   **`handlers/messaging_handler.js`** — Прием входящих WhatsApp/SMS (и чеков).
*   **`calendar/calendarTools.js`** — **ГЛАВНЫЙ ФАЙЛ ИНСТРУМЕНТОВ**. Все функции для Gemini (проверка, бронь, отправка сообщений).
*   **`calendar/calendarService.js`** — Низкоуровневая работа с Google API (проверка синонимов яхт).
*   **`data/botBehavior.js`** — **МОЗГИ (Промпт)**. Системная инструкция, личность бота, правила поведения.
*   **`data/products_knowledge_base.csv`** — База данных товаров (Яхты, Цены, Ссылки, Локации).
*   **`scripts/loadDocuments.js`** — Скрипт очистки и загрузки базы в ChromaDB.
*   **`utils/whatsappService.js`** — Отправка WhatsApp (с поддержкой Медиа).
*   **`utils/smsService.js`** — Отправка SMS.

---

## 🚑 Инструкции по устранению проблем

### 1. Дубликаты в поиске / База "глючит"
Решение: Полная пересборка контейнера.
```powershell
docker stop chromadb
docker rm chromadb
docker run -d --name chromadb -p 8000:8000 chromadb/chroma
node scripts/loadDocuments.js
pm2 restart all
